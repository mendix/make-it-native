git_url("git@ssh.gitlab.rnd.mendix.com:mobile-offline/ios-credentials.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["com.mendix.developerapp.native.mx9"])

# clone_branch_directly(true) 

gitlab_host("gitlab.rnd.mendix.com")
# The docs are available on https://docs.fastlane.tools/actions/match
